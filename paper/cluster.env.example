# Optional overrides for cluster benchmark scripts.
# Copy the required entries into your shell profile or job environment.

export MLLMCELLTYPE_SCRATCH_ROOT="/scratch/user/${USER:-$(id -un)}"
export MLLMCELLTYPE_SCANVI_ROOT="${MLLMCELLTYPE_SCRATCH_ROOT}/scanvi_benchmark"
export MLLMCELLTYPE_SINGLER_ROOT="${MLLMCELLTYPE_SCRATCH_ROOT}/singler_sctype_batch"
export MLLMCELLTYPE_R_LIB="${MLLMCELLTYPE_SCRATCH_ROOT}/R_libs"
export MLLMCELLTYPE_PAPER_ROOT="/path/to/mLLMCelltype/paper"
export MLLMCELLTYPE_PYTHON="python3"
