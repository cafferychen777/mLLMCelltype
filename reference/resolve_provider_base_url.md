# URL Utilities for Base URL Resolution

This file contains utility functions for resolving and validating custom
base URLs for different API providers. Resolve provider-specific base
URL

## Usage

``` r
resolve_provider_base_url(provider, base_urls)
```
