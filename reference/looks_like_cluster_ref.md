# Whether a label key denotes a cluster id (bare number or "Cluster N")

Whether a label key denotes a cluster id (bare number or "Cluster N")

## Usage

``` r
looks_like_cluster_ref(key)
```

## Arguments

- key:

  Parsed label key.

## Value

`TRUE` when the key is an explicit cluster reference.
