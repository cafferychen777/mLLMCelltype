% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/litellm_processor.R
\name{list_litellm_models}
\alias{list_litellm_models}
\title{List models served by a LiteLLM gateway}
\usage{
list_litellm_models(api_key = NULL, base_url = NULL)
}
\arguments{
\item{api_key}{Gateway key. Falls back to the \code{LITELLM_API_KEY} environment
variable, then to an unauthenticated request, which works for a gateway
started without a master key.}

\item{base_url}{Optional custom gateway endpoint}
}
\value{
Character vector of model ids served by the gateway
}
\description{
The reachable model set is whatever the gateway operator configured, so it is
discovered rather than hardcoded. Useful for checking which models are
available before assembling a consensus run.
}
